@model IEnumerable<ClinicaVeterinariaApp.Models.Exams>


<table class="table">
    <tr>
       
        <th>
            @Html.DisplayNameFor(model => model.ExamDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Exam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExamNotes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @*@Html.DisplayFor(modelItem => item.ExamDate,new { @format = "d" })*@
            <span>@item.ExamDate.ToString("d")</span>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Exam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExamNotes)
        </td>
        <td>
            @Html.ActionLink("Aggiorna", "Edit", new { id=item.ExamID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExamID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExamID })
        </td>
    </tr>
}

</table>
